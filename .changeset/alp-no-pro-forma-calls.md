---
'@runpod/mcp-server': patch
---

The ALP briefing and the `ask_question` / `report_feedback` descriptions now say plainly that these tools are for real observations only: never call one to satisfy a requirement to use a tool, never with placeholder content, and calling nothing is the correct move when there is nothing to report. One Agent SDK app that forces a tool call every turn produced 150 "placeholder" questions in half an hour.
