---
'@runpod/mcp-server': patch
---

Detect input-contract drift, including request bodies, parameters and transitively referenced schemas, even when operation routes are unchanged. Preserve complete log entries when a snapshot ends with an unfinished SSE heartbeat or metadata block.
